#include <stdio.h>
#include <complex.h>
#include "fft.h"

#define NUMELEMS(x) (sizeof(x) / sizeof((x)[0]))
#define INTMAX sizeof(uint_t)
int main()
{
    uint_t x = 0xbcd178;
    uint_t len = 16777216;
    for(int i = 0; i < 4096; i++)
    {

        printf("%ull",INTSIZE);
    }
    return 0;
}
